#include<bits/stdc++.h>
#define ascii 256
using namespace std;
char getmaxoccuring(char *str)
{
	int count[ascii]={0};
	int len = strlen(str);
	for(int i=0;i<len;i++)
	  count[str[i]]++;
	int max=-1;
	  
}
